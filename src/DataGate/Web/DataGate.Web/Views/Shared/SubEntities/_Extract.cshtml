@using DataGate.Common

@{
    var controller = this.ViewContext.RouteData.Values["controller"].ToString();
    var route = string.Empty;

    if (controller == GlobalConstants.FundSubFundsControllerName)
    {
        route = $"/f/{Model.Id}/{Model.Date}";
    }
}

<div>
    <div class="d-flex justify-content-between h-70">
        <div>
            <button class="btn btn-sm btn-primary btn-media"
                    type="submit"
                    asp-area=""
                    asp-controller="Media"
                    asp-action="Download"
                    name="Command"
                    value="Excel">
                Excel
            </button>
        </div>

        <div class="ml-4">
            <button class="btn btn-sm btn-primary btn-media"
                    type="submit"
                    asp-area=""
                    asp-controller="Media"
                    asp-action="Download"
                    name="Command"
                    value="PDF">
                PDF
            </button>
        </div>
    </div>

    <input type="hidden" name="ControllerName" value="@controller" />
    <input type="hidden" name="RouteName" value="@route" />
</div>

<div class="vertical mx-3"></div>