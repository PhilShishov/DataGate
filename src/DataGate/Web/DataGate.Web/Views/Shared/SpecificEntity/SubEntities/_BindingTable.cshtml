@model DataGate.Web.ViewModels.Entities.SpecificEntityViewModel

<div>
    <table class="table" name="EntitySubEntities" hidden>
        <thead>
            <tr>
                @for (int row = 0; row < 1; row++)
                {
                    @for (int col = 0; col < Model.Headers[row].Length; col++)
                    {
                        <th>
                            <h5>
                                @Html.TextBoxFor(af => Model.Headers[row][col],
                                    new
                                         {
                                        @readonly = true
                                    })
                            </h5>
                        </th>
                    }
                }
            </tr>
        </thead>
        <tbody>
            @for (int row = 1; row < Model.Values.Count; row++)
            {
                <tr>
                    @for (int col = 0; col < Model.Values[row].Length; col++)
                    {
                        <td>
                            @Html.TextBoxFor(af => Model.Values[row][col],
                                new
                                     {
                                    @readonly = true
                                })
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
</div>