@*@using DataGate.Common
@model DataGate.Web.ViewModels.Entities.SpecificEntityViewModel

@{
    var controller = this.ViewContext.RouteData.Values[GlobalConstants.ControllerRouteDataValue].ToString();
    var targetRoute = string.Empty;

    if (controller == GlobalConstants.FundSubEntitiesControllerName)
    {
        targetRoute = "sf";
    }

    var entityId = string.Empty;
}

<div class="table-responsive border-0 mx-auto" id="tableDoubleScroll" name="EntitySubEntities">
    <table class="table table-hover text-nowrap">
        <thead>
            @for (int row = 0; row < 1; row++)
            {
                <tr>
                    @for (int col = 0; col < Model.EntitySubEntities[row].Length; col++)
                    {
                        <th class="text-white p-1 tableHeaderPharus">
                            @Model.EntitySubEntities[row][col]
                        </th>
                    }
                </tr>
            }
        </thead>

        <tbody>
            @for (int row = 1; row < Model.EntitySubEntities.Count; row++)
            {
                entityId = Model.EntitySubEntities[row][2];

                <tr class="tableCellPharus" onclick="document.location = '/@targetRoute/@entityId/@Model.Date'">
                    @for (int col = 0; col < Model.EntitySubEntities[row].Length; col++)
                    {
                        <td class="p-1">
                            @Model.EntitySubEntities[row][col]
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
</div>*@