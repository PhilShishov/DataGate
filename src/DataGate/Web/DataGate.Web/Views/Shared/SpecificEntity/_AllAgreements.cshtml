@*@model DataGate.Web.ViewModels.Entities.SpecificEntityViewModel
@{
    var controllerName = this.ViewContext.RouteData.Values["controller"].ToString();
}

<div id="allAgreements" class="mt-3 d-none">
    <table class="table table-bordered table-hover">
        <thead>
            @for (int row = 0; row < 1; row++)
            {
                <tr>
                    @for (int col = 0; col < Model.Agreements[row].Length; col++)
                    {
                        <th class="text-secondary p-1">
                            @Model.Agreements[row][col]
                        </th>
                    }
                </tr>
            }
        </thead>

        <tbody>
            @for (int row = 1; row < Model.Agreements.Count; row++)
            {
                <tr>
                    @for (int col = 0; col < Model.Agreements[row].Length; col++)
                    {
                        <td class="p-1">
                            @if (Model.Agreements[0][col] == "File Name")
                            {
                                <button class="btn-link-pharus text-left"
                                        formtarget="_blank"
                                        type="submit"
                                        formaction="/@controllerName/ReadAgreement"
                                        name="pdfValue"
                                        value="@Model.Agreements[row][col]">
                                    @System.IO.Path.GetFileNameWithoutExtension(Model.Agreements[row][col])
                                </button>
                            }
                            else
                            {
                                @Model.Agreements[row][col]
                            }
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
</div>*@